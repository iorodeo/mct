<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    <include file="{{machine_file}}" />
    {% for region, topic in launch_list %}
    <group ns="{{region}}">
        <node pkg="mct_logging" type="tracking_pts_logger.py" name="tracking_pts_logger" args="{{topic}}" machine="mct_master" />
    </group>
    {% endfor %} 
</launch>

