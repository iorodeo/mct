<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    <include file="{{machine_file}}" />
    {% for namespace, computer in launch_list %}
    <group ns="{{namespace}}">
        <node pkg="image_proc" type="image_proc" name="image_proc" machine="{{computer}}"/>
    </group>
    {% endfor %} 
</launch>
