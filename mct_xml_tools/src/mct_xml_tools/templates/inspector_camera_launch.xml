<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    {% for guid, info in camera_dict.iteritems() %}
    <group ns="/{{info['machine']}}/guid{{guid}}" >
        <node pkg="camera1394" type="camera1394_node" name="camera1394_node" machine="{{info['machine']}}" >
            <rosparam file="$(env MCT_CONFIG)/camera/camera_common.yaml"/>
            <rosparam file="/tmp/camera_guid{{guid}}.yaml" />
        </node>
    </group>
    {% endfor %}
</launch>
