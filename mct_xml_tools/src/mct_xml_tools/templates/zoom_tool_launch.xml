<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    <include file="{{machine_file}}" />

    <group ns="zoom_tool_params">
        <rosparam command="load" file="{{params_file}}"/>
    </group>
    {% for namespace, image_topic, computer in launch_list %}
    <group ns="{{namespace}}">
        <node pkg="mct_zoom_tool" type="zoom_tool_node.py" name="zoom_tool" args="{{image_topic}}" machine="{{computer}}" >
        </node>
    </group>
    {% endfor %} 
</launch>
