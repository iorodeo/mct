<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    <include file="{{machine_file}}" />
    {% for node_name, node_args in launch_list %}
    <node pkg="tf" type="static_transform_publisher" name="{{node_name}}" args="{{node_args}}" machine="mct_master" />
    {% endfor %} 
</launch>
