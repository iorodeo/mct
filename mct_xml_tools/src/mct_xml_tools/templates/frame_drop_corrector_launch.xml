<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    <include file="{{machine_file}}" />
    {% for namespace, topic, machine in launch_list %}
    <group ns="{{namespace}}">
        <node pkg="mct_frame_drop_corrector" type="frame_drop_corrector.py" name="frame_drop_corrector" args="{{topic}} {{framerate}}" machine="{{machine}}" />
    </group>
    {% endfor %} 
</launch>
