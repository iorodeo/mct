<launch>
    <!-- Autogenerated launch file - do not hand edit -->
    <include file="{{machine_file}}" />

    {% for namespace, topic in launch_list %}
    <group ns="{{namespace}}">
        <node pkg="mct_avi_writer" type="avi_writer_node.py" name="avi_writer" args="{{topic}}" machine="mct_master" />
    </group>
    {% endfor %} 
</launch>
